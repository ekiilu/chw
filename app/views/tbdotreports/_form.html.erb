<%= form_for(@tbdotreport) do |f| %>
  <% if @tbdotreport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tbdotreport.errors.count, "error") %> prohibited this tbdotreport from being saved:</h2>

      <ul>
      <% @tbdotreport.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
