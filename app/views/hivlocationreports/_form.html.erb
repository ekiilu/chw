<%= form_for(@hivlocationreport) do |f| %>
  <% if @hivlocationreport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hivlocationreport.errors.count, "error") %> prohibited this hivlocationreport from being saved:</h2>

      <ul>
      <% @hivlocationreport.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
