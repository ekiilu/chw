<%= form_for(@malarialocationreport) do |f| %>
  <% if @malarialocationreport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@malarialocationreport.errors.count, "error") %> prohibited this malarialocationreport from being saved:</h2>

      <ul>
      <% @malarialocationreport.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
