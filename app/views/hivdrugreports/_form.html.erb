<%= form_for(@hivdrugreport) do |f| %>
  <% if @hivdrugreport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hivdrugreport.errors.count, "error") %> prohibited this hivdrugreport from being saved:</h2>

      <ul>
      <% @hivdrugreport.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
