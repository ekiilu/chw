<%= form_for(@tbcase) do |f| %>
  <% if @tbcase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tbcase.errors.count, "error") %> prohibited this tbcase from being saved:</h2>

      <ul>
      <% @tbcase.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date_reported %><br />
    <%= f.date_select :date_reported %>
  </div>
  <div class="field">
    <%= f.label :patient_number %><br />
    <%= f.text_field :patient_number %>
  </div>
  <div class="field">
    <%= f.label :availability_of_drugs %><br />
    <%= f.text_field :availability_of_drugs %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
