<%= form_for(@malariapreventationreport) do |f| %>
  <% if @malariapreventationreport.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@malariapreventationreport.errors.count, "error") %> prohibited this malariapreventationreport from being saved:</h2>

      <ul>
      <% @malariapreventationreport.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
